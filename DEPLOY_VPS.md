# دليل تنصيب نظام إدارة العيادة على سيرفر VPS

هذا الدليل يشرح لك كيفية تنصيب النظام على سيرفر Windows أو Linux ليعمل بشكل دائم.

## المتطلبات الأساسية
1. تثبيت **Node.js** (إصدار 18 فما فوق).
2. تثبيت **PM2** (اختياري ولكن مادي لإبقاء النظام يعمل دائماً):
   ```bash
   npm install pm2 -g
   ```

## خطوات التنصيب
1. **تحميل الملفات**: ارفع ملف الـ ZIP للسيرفر وقم بفك الضغط.
2. **تثبيت الملحقات**: افتح التيرمنال (CMD أو PowerShell) داخل مجلد المشروع ونفذ:
   ```bash
   npm install
   ```
3. **بناء النظام (Build)**: قم بتحويل الأكواد البرمجية لنسخة قابلة للتشغيل:
   ```bash
   npm run build
   ```
4. **تشغيل النظام**: 
   - للتشغيل العادي: `npm start`
   - للتشغيل الدائم عبر PM2 (الموصى به):
     ```bash
     pm2 start ecosystem.config.cjs
     pm2 save
     ```

## كيف تصل للنظام بعد التنصيب؟
بمجرد تشغيل النظام، يمكنك الدخول إليه من أي متصفح عبر الـ IP الخاص بالسيرفر على بورت 3000:
`http://SERVER_IP:3000`

---
**ملاحظة**: تأكد من فتح بورت (Port 3000) في جدار الحماية (Firewall) الخاص بالسيرفر لتتمكن من الوصول إليه.
